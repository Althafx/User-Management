<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cloud Gallery - Text Editor</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(120deg, #4e54c8, #8f94fb);
            height: 100vh;
            margin: 0;
            display: flex;
            flex-direction: column;
            font-family: Arial, sans-serif;
            background-image: url("https://cdn.pixabay.com/photo/2023/11/18/23/27/red-8397397_960_720.png");
            background-repeat: no-repeat;
            background-size: cover;
            background-attachment: fixed;

        }
        .navbar {
            background-color: #000000;
        }
        .navbar-brand {
            color: white;
            font-weight: bold;
        }
        .navbar-brand:hover {
            color: #a30303;
        }
        .logout-btn {
            background: #a30303;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
        }
        .logout-btn:hover {
            background: #d44444;
        }
        .editor-container {
            margin: 30px auto;
            width: 90%;
            max-width: 800px;
        }
        .editor-container h2 {
            color: #fff;
            text-align: center;
            margin-bottom: 20px;
        }
        .text-editor {
            width: 100%;
            height: 300px;
            padding: 15px;
            border: none;
            border-radius: 10px;
            background: #fff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            font-size: 16px;
            resize: none;
            outline: none;
        }
        .btn-custom {
            background:  #750909;
            color: white;
            border: none;
            transition: 0.3s;
            display: block;
            margin: 20px auto;
            padding: 10px 20px;
        }
        .btn-custom:hover {
            background: #d44444;
            color: white;
        }

    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container d-flex justify-content-between">
            <a class="navbar-brand" href="/userhome">Cloud Text Editor</a>
            <!-- Logout Button -->
            <form action="/logout" method="GET" style="margin: 0;">
                <button type="submit" class="logout-btn">Logout</button>
            </form>
        </div>
    </nav>
    <div class="editor-container">
        <h2>Welcome, {{username}}! Write Your Thoughts Below</h2>
        <form>
            <textarea name="content" class="text-editor" placeholder="Start typing your thoughts here...">{{content}}</textarea>
            <button type="submit" class="btn btn-custom">Save</button>
        </form>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
